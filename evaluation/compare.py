# “Write Python code to compare two setups:
# (a) supervised only training,
# (b) semi-supervised active learning.
# Run both on the same stream, collect metrics from metrics.py, and plot with plots.py.”