# NIKHITHA
# “Write Python code with two functions:
# ssl_train_step(model, X_labeled, y_labeled, X_unlabeled) – combine supervised cross-entropy + SSL smoothness loss.
# train_step(model, X_labeled, y_labeled, X_unlabeled) – incremental training with active learning batches.
# Return loss values.”