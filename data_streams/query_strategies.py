# SANJANA
# "Write Python code implementing four query strategies:
# uncertainty_query(probs, k) – select samples closest to probability 0.5.
# hybrid_query(probs, k, alpha=0.5) – mix uncertainty and random.
# chunk_cluster_query(X, k) – cluster each batch with KMeans, select one per cluster.
# weighted_chunk_query(X, k) – extend chunk clustering by weighting cluster size.
# Return indices of selected samples.”