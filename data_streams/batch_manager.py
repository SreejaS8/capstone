# SANJANA
# “Write Python code for a function get_training_batch(stream_chunk, strategy) that applies a query strategy from query_strategies.py and returns (X_labeled, y_labeled, X_unlabeled). Save labeled/unlabeled splits in datasets/batches/ as .npz files for later model training.”
